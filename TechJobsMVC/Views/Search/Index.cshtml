<div class="container body-content">

    <h1>@ViewBag.title</h1>

    <form action="/search/results" method="post">
        <h2>Search by:</h2>

        <p>
            @foreach (var column in ViewBag.columns)
            {
                if (column.Key == ViewBag.searchType)
                {
                    <span>
                        <input type="radio" name="searchType" id="@column.Key" value="@column.Key" checked />
                        <label for="@column.Key">@column.Value</label>
                    </span>
                }
                else
                {
                    <span>
                        <input type="radio" name="searchType" id="@column.Key" value="@column.Key" />
                        <label for="@column.Key">@column.Value</label>
                    </span>
                }
            }

        </p>

        <p>
            <label for="searchTerm">Keyword:</label>
            <input type="text" name="searchTerm" id="searchTerm" />
        </p>

        <input type="submit" value="Search" />
    </form>

    <hr />

    @if (ViewBag.jobs != null)
    {

        @foreach (Job job in ViewBag.jobs)
        {
            
            <table class="job-listing">
                <tr>
                    <td>ID: @job.Id</td>
                    <td>Name: @job.Name</td>
                    <td>Employer: @job.Employer</td>
                    <td>Location: @job.Location</td>
                    <td>Position Type: @job.PositionType</td>
                    <td>Skill: @job.CoreCompetency</td>
                </tr>

            </table>
        }
    }



    @{
        //TODO #4: Loop over the search results to display all job fields.
    }

</div>


@{
    // Copied from Jobs view.
}

@*<div class="container body-content">

        <h3>@ViewBag.title</h3>

        @foreach (Job job in ViewBag.jobs)
        {
            <table class="job-listing">
                <tr>
                    <td>ID: @job.Id</td>
                    <td>Name: @job.Name</td>
                    <td>Employer: @job.Employer</td>
                    <td>Location: @job.Location</td>
                    <td>Position Type: @job.PositionType</td>
                    <td>Skill: @job.CoreCompetency</td>
                </tr>

            </table>
        }





    </div>*@